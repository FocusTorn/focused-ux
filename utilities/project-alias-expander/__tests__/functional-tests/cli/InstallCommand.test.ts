import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest'

describe('InstallCommand Tests', () => {

    beforeEach(() => {})

    afterEach(() => {})

    //it should handle all install command variations individually tested

})
