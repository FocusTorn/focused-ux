const EXT_ID_PREFIX = 'fux-<%= name %>'

export const constants = {
	extension: {
		name: 'F-UX: <%= pascalCaseName %>',
		id: EXT_ID_PREFIX,
		configKey: EXT_ID_PREFIX,
	},
	commands: {
		// someCommand: `${EXT_ID_PREFIX}.someCommand`,
	},
	configKeys: {
		// someConfig: 'some.config',
	},
} as const