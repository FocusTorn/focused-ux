import { beforeEach, afterEach } from 'vitest'

// Global test setup for ghost-writer extension package
beforeEach(() => {
  // Clear any test state
})

afterEach(() => {
  // Cleanup after each test
}) 