/**
 * Global test setup for <%= className %>
 */

import { beforeAll, afterAll } from 'vitest';

// Global test setup
beforeAll(async () => {
  // Setup any global test dependencies
  console.log('Setting up tests for <%= className %>');
});

// Global test cleanup
afterAll(async () => {
  // Cleanup any global test resources
  console.log('Cleaning up tests for <%= className %>');
});
