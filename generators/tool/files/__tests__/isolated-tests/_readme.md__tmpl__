# Isolated Tests

This directory contains unit tests that test individual components in isolation.

## Purpose

- Test individual functions and classes
- Verify unit behavior in isolation
- Test edge cases and error conditions
- Ensure proper mocking and stubbing

## Structure

- `*.test.ts` - Unit test files
- `*.spec.ts` - Unit specification tests
- `_setup.ts` - Unit test setup (if needed)
