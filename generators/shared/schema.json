{
    "$schema": "http://json-schema.org/schema",
    "$id": "SharedPackage",
    "title": "Create a shared package",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "description": "The name of the shared package (e.g., 'utilities', 'common')",
            "$default": {
                "$source": "argv",
                "index": 0
            },
            "x-prompt": "What is the name of the shared package?"
        },
        "description": {
            "type": "string",
            "description": "Description of the shared package",
            "x-prompt": "What is the description of this shared package?"
        },
        "directory": {
            "type": "string",
            "description": "The directory where the package should be created",
            "default": "packages"
        }
    },
    "required": ["name", "description"]
}
