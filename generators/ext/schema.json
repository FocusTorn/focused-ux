{
    "$schema": "http://json-schema.org/schema",
    "$id": "ExtensionPackage",
    "title": "Create a VSCode extension package",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "description": "The name of the extension package (e.g., 'ghost-writer', 'project-butler')",
            "$default": {
                "$source": "argv",
                "index": 0
            },
            "x-prompt": "What is the name of the extension package?"
        },
        "displayName": {
            "type": "string",
            "description": "The display name for the VSCode extension",
            "x-prompt": "What is the display name for this extension?"
        },
        "description": {
            "type": "string",
            "description": "Description of the extension",
            "x-prompt": "What is the description of this extension?"
        },
        "corePackage": {
            "type": "string",
            "description": "The name of the core package this extension depends on",
            "x-prompt": "What is the name of the core package this extension depends on?"
        },
        "directory": {
            "type": "string",
            "description": "The directory where the package should be created",
            "default": "packages"
        }
    },
    "required": ["name", "displayName", "description", "corePackage"]
}
