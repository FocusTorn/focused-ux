{
    "$schema": "http://json-schema.org/schema",
    "$id": "TestScaffold",
    "title": "Scaffold test directory structure",
    "description": "Generate a comprehensive test directory structure with Mockly integration, vitest configuration, and helper utilities for VSCode extension testing",
    "type": "object",
    "properties": {
        "project": {
            "type": "string",
            "description": "The name of the project to add tests to",
            "$default": {
                "$source": "argv",
                "index": 0
            },
            "x-prompt": "What is the name of the project to add tests to?"
        },
        "packageType": {
            "type": "string",
            "description": "The type of package (core, ext, shared, lib) - affects test target configuration",
            "enum": ["core", "ext", "shared", "lib"],
            "default": "core",
            "x-prompt": "What type of package is this?"
        },
        "includeHelpers": {
            "type": "boolean",
            "description": "Whether to include a helpers.ts file with common test utilities and mock setup functions",
            "default": true
        },
        "includeVitestConfigs": {
            "type": "boolean",
            "description": "Whether to include vitest functional and coverage configs with proper VSCode extension testing setup",
            "default": true
        }
    },
    "required": ["project"],
    "examples": [
        {
            "description": "Generate test scaffold for a core package",
            "project": "my-feature",
            "packageType": "core"
        },
        {
            "description": "Generate test scaffold for an extension package",
            "project": "my-feature-ext",
            "packageType": "ext"
        },
        {
            "description": "Generate test scaffold for a shared library",
            "project": "utilities",
            "packageType": "shared"
        }
    ]
} 