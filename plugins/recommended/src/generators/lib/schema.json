{
    "$schema": "https://json-schema.org/schema",
    "$id": "Lib",
    "title": "Library Generator",
    "description": "Generate a new shared library in the libs directory",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "description": "The name of the library to create",
            "$default": {
                "$source": "argv",
                "index": 0
            },
            "x-prompt": "What name would you like to use for the library?"
        },
        "description": {
            "type": "string",
            "description": "Description of the library's purpose",
            "x-prompt": "What is the purpose of this library?"
        },
        "tags": {
            "type": "string",
            "description": "Tags to apply to the library (comma-separated)",
            "x-prompt": "What tags should be applied to this library? (e.g., scope:shared,type:utility)"
        }
    },
    "required": ["name"]
}
